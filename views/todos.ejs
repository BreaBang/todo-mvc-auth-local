<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body class="todos-body">
<section class="todos">
    <section class="todos-heading">
    <h1>Todos</h1>
    </section>
    <section class="todo-items">
    <ul>
    <% todos.forEach( el => { %>
            <li class='todoItem' data-id='<%=el._id%>'>
                <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.todo %></span>
                <span class='del'> Delete </span>
                <span>Author: <%=  %></span>
            </li>
    <% }) %>    
    </ul>

    <h2><%= user.userName %> has <%= left %> things left to do.</h2>

    <form action="/todos/createTodo" method='POST' class="todos-formbox">
        <input type="text" placeholder="Enter Todo Item" name='todoItem' class="formbox">
        <input type="submit" class="btn-submit">
    </form>
    </section>
    <section class="logout-section">
    <a href="/logout" class="btn logout">Logout</a>
    </section>
    </section>
    <script src="js/main.js"></script>
</body>
</html>